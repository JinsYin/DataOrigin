<settings>
	<servers>
		<!-- ./mvnw clean deploy -Dcentral.username=<username> -Dcentral.password=<password> -->
		<!-- see https://central.sonatype.com/account -->
		<server>
			<id>central</id>
			<!-- use user token authentication -->
			<username>${central.username}</username>
			<password>${central.password}</password>
		</server>
	</servers>
</settings>
